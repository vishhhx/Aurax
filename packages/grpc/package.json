{
  "name": "@repo/grpc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "generate": "proto-loader-gen-types --longs=String --enums=String --defaults --oneofs --grpcLib=@grpc/grpc-js --outDir=generated proto/*.proto",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "proto-loader-gen-types": "^1.x",
    "typescript": "^5"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.x",
    "@grpc/proto-loader": "^0.x"
  }
}
